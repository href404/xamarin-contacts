<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Contacts.Views.DisplayContactView">
    <ContentPage.Content>
        <StackLayout Margin="10" Spacing="10">
            <Label Text="{Binding Model.LastName, Mode=TwoWay}" />
            <Label Text="{Binding Model.FirstName, Mode=TwoWay}" />
            <Button Text="Edit" HorizontalOptions="Center" Command="{Binding EditCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>